Place your existing private app template here. This folder is intentionally not populated with bot code: the launch console will clone everything inside it into a new deployment.

Follow the required layout and configuration markers in the parent README.
